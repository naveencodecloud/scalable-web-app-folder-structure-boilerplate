// import express from "express";
// import cors from "cors";

// // App Config
// const app = express();
// const port = 4000;

// app.use(express.json()); // allow you parse json from incoming request
// app.use(cors());


// app.get("/", (req, res) => {
//     res.send("ghost");
// });

// app.listen(port, () => console.log("server is runnning on port - " + port));
